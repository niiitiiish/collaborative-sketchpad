services:
  - type: web
    name: simple-sketchpad
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /
    autoDeploy: true
    plan: free 