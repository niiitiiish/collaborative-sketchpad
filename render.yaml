services:
  - type: web
    name: collaborative-sketchpad
    env: node
    rootDir: .
    buildCommand: |
      npm install
      cd client
      npm install
      npm run build
      cd ..
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000 